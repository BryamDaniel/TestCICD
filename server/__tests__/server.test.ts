// server/__tests__/server.test.ts
describe('Server Tests', () => {
  test('Sanity check should pass', () => {
    // Si esta prueba pasa, la configuración de Jest/TS es correcta.
    expect(true).toBe(true);
  });
});